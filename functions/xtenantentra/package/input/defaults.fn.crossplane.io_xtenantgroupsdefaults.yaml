---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: xtenantgroupsdefaults.defaults.fn.crossplane.io
spec:
  group: defaults.fn.crossplane.io
  names:
    categories:
    - crossplane
    kind: XTenantGroupsDefaults
    listKind: XTenantGroupsDefaultsList
    plural: xtenantgroupsdefaults
    singular: xtenantgroupsdefaults
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Input can be used to provide input to this Function.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          argocdApp:
            description: The ArgoCD App Registration and Enterprise Application that
              enables SSO for users.
            properties:
              appRegObjectID:
                description: The ObjectID of the ArgoCD App Registration
                minLength: 1
                type: string
              enterpriseAppObjectID:
                description: The ObjectID of the ArgoCD Enterprise Application
                minLength: 1
                type: string
            required:
            - appRegObjectID
            - enterpriseAppObjectID
            type: object
          gcpAppSCIM:
            description: The GCP Enterprise Application that enables SCIM Provisioning
              for users.
            properties:
              objectID:
                description: The ObjectID of the Enterprise Application
                minLength: 1
                type: string
              roleID:
                description: The "Default Organization" role on the Enterprise Application
                minLength: 1
                type: string
            required:
            - objectID
            - roleID
            type: object
          gcpAppSSO:
            description: The GCP Enterprise Application that enables SSO for users.
            properties:
              objectID:
                description: The ObjectID of the Enterprise Application
                minLength: 1
                type: string
              roleID:
                description: The "Default Organization" role on the Enterprise Application
                minLength: 1
                type: string
            required:
            - objectID
            - roleID
            type: object
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          providerConfigRef:
            properties:
              kind:
                description: ProviderConfig kind
                minLength: 1
                type: string
              name:
                description: The name of the ProviderConfig to use
                minLength: 1
                type: string
            required:
            - kind
            - name
            type: object
        required:
        - argocdApp
        - gcpAppSCIM
        - gcpAppSSO
        - providerConfigRef
        type: object
    served: true
    storage: true
