---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: inputs.defaults.fn.crossplane.io
spec:
  group: defaults.fn.crossplane.io
  names:
    categories:
    - crossplane
    kind: Input
    listKind: InputList
    plural: inputs
    singular: input
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Input can be used to provide input to this Function.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          argocdApp:
            description: ArgoCDApp holds the ArgoCD App Registration and Enterprise
              Application IDs.
            properties:
              appRegObjectID:
                description: AppRegObjectID is the object ID of the ArgoCD App Registration.
                minLength: 1
                type: string
              enterpriseAppObjectID:
                description: EnterpriseAppObjectID is the object ID of the ArgoCD
                  Enterprise Application.
                minLength: 1
                type: string
            required:
            - appRegObjectID
            - enterpriseAppObjectID
            type: object
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          providerConfigRef:
            description: ProviderConfigRef references the AzureAD ProviderConfig to
              use.
            properties:
              kind:
                description: Kind is the kind of the ProviderConfig.
                minLength: 1
                type: string
              name:
                description: Name is the name of the ProviderConfig to use.
                minLength: 1
                type: string
            required:
            - kind
            - name
            type: object
          vaultApp:
            description: VaultApp holds the Vault App Registration and Enterprise
              Application IDs.
            properties:
              appRegObjectID:
                description: AppRegObjectID is the object ID of the Vault App Registration.
                minLength: 1
                type: string
              enterpriseAppObjectID:
                description: EnterpriseAppObjectID is the object ID of the Vault Enterprise
                  Application.
                minLength: 1
                type: string
            required:
            - appRegObjectID
            - enterpriseAppObjectID
            type: object
        required:
        - argocdApp
        - providerConfigRef
        - vaultApp
        type: object
    served: true
    storage: true
